import React from 'react';
import s from './ImageFullWide.css';
import Image from '../Image/Image';

const ImageFullWide = (props) => <Image className={s.imageFullWide} {...props} />;

export default ImageFullWide;
